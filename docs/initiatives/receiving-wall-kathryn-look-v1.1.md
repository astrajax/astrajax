# Receiving Wall — Kathryn look delta v1.1

**Status:** Working, 13 Aug 2026. Not final. TL / Kathryn keep finish after Matthew sees the next draft.  
**For:** Kate, branch `cursor/receiving-wall-operator-portals-f54c`. Apply now. Structure (doors, camera, data) stays.

Matthew rejected traditional UI on the v1 draft. This is a delta against the shipped look file `receiving-wall-kathryn-look-v1.md`.

## Clive in the aperture

Kill `.popOverlay` / `.popPanel` (cream drawer, blur, round close, “Guided curation”). Clive speaks inside `.bayTravel > .bayContent` with `CliveChatSurface` `studyMode` (no bubbles). Idle “Sit with Clive” uses the existing dolly; the bench behaves as a fourth door. No folio deckle plate, portrait, or mascot in the arch. Retint study chrome to wall `--tint` / `.letterBody` / `.letterMeta`. Writing line = existing letter double hairline. Send = `.incisedAction`. Cut starter pills.

## Letter versus list

A list is a roll of names (no helper sentence). A letter is one thing opened (body + at most two actions). Kill `.recordChevron`. Kill judgement `.baySectionHint`s. Kill “Fold the letter” as a button (row + Escape close). Operator heads: `NEEDS YOU` / `HELD BACK` / `PROPOSED OVERNIGHT`. Empty: “Nothing waiting.” / “Someone stopped this. Read why.” / “Nothing proposed overnight.” Drop `agentSlug` from the ledger. Letter meta: two facts only (where it came from, where it wants to go).

## Shrine

Radial-gradient mask is **working, awaiting wall shrine matte** — mark it in CSS; do not treat as finish. Cut `.shrineTheme`. Name, state word, one way in. Contact darkening at the loop base. Band colour on the word only.

## Type receding

Bay title ~`0.040` of `--ap-base-w`. Kill `.zoomBlurb` when zoomed. `.backBtn` into `.incisedAction`. Honesty note below the doors at `.benchKicker` weight.

## Keep

Four-shadow incision, `.letter` hairline, `letterIn`, `.acceptBtn` dominance, `.heldLip` terracotta once, `--band-tint`, shared travel, sill hotspot, varnish stack, `To the brains →`, seed fallback.
