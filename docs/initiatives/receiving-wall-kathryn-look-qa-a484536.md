# Receiving Wall — Kathryn look QA (`a484536`)

**Status:** QA, 13 Aug 2026. Not final. Preview was behind Vercel login from the QA environment; judged on code.

## Must-fix before showing Tara-Lee

1. Held-empty copy: `"Someone stopped this. Read why."` when nothing is held is a lie. Use `"Nothing held back."`
2. Shrine too small + framed `/brain/state-*.png` poster. Raise `.shrineLoop` toward `min(46%, 26rem)`; drop square lock; unframed still for poster / reduced motion.
3. Terracotta `#D77545` once per bay — on the held section, not every held row.
4. Drop the `0 1px 2px` blur shadows on `.letterBody`, `.letterLead`, `.note`. Use the two-tone cut.
5. Jar grain double-dip: `.shrineVarnish` 0.14 → 0.05.

Also: honesty line should not be a wide uppercase strip saying “This bay” at idle; `--focus-ring` to `#E4D3A3`; no hover box on the painted sill letter.

## Working — leave for finish

Door scale, three right-hand marks, travelling interior, reports letter-first, Clive in the aperture, honesty position/weight, shrine feather as placeholder until a real wall matte.

## Not Kathryn’s call

Settled dolly 1.22 vs spec crop of sconces. Nave / `.aperture` phone path — Doc or Kate on a real phone.
