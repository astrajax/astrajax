# Receiving Wall — Kathryn working look (v1)

**Status:** Working direction, 13 Aug 2026. Not final. TL / Kathryn keep finish authority after Matthew sees the draft.  
**For:** Kate, same branch `cursor/receiving-wall-operator-portals-f54c`. Apply without another human pause.

## Idle doors

Three inscriptions, not a shortened category list. Door names at roughly `.zoomTitle` scale: `clamp(1.6rem, calc(var(--ap-base-w) * 0.052), 2.6rem)`. Gap `clamp(2.5rem, 7vh, 4.5rem)`. Keep the existing four-shadow incision on `.sourceName`. Type never scales with `--dolly`.

Each door: caps name + italic operator sub-line + a **different** right-hand mark.

| Door | Right-hand slot |
|---|---|
| Judgement | numeral (how many wait) |
| Brain health | state word (worst band present) |
| This morning | time word (`Today`, or the date) |

Working copy (Matthew owns final wording):

```
JUDGEMENT     What waits on you.              7
THE BRAINS    How the household is holding.   Okay
THIS MORNING  What the house did overnight.   Today
```

Order: Judgement, Brains, This morning. Doors never disappear when empty. Hover: existing 4px shift only — no scale, glow, or underline.

## Health

One shrine loop at a time (worst band default), upper aperture ~source y 22%–55%, ~38–46% of safe-box width, `object-fit: contain`. `shrineArtForBand()` for the loop, `stillArtForBand()` as poster / reduced-motion. Dissolve all four sides; `.varnishGrain` over it. Band colour is a hairline catch on the word, never a room fill, never five thumbnails. Click-through uses existing `To the brains →` grammar. Do not import BrainWorkspace chrome.

## Letter vs ledger

- **Judgement:** ledger first, letter second. Section heads in `.letterMeta` family with a short scored hairline. Only “needs a human” opens a letter with Accept + Discuss with Clive. Held = read-only letter, no Accept. V1 proposals = one line, no open/execute in v1.
- **Reports:** letter first (daily change summary already unfolded). No inner scroll box. Sibling Auditor/Challenger reports as a short ledger below.
- **Health:** neither ledger nor letter.
- **Sill letter hotspot:** same door as This morning; tint focus ring at 5px offset; accessible name matches the inscription; no glow on the painted letter.

## Refuse

Cards, pills, neon, tabs, mascot in the arch, play/run controls, traffic-light chips, buttermilk as a surface, hijacking `/brain` chrome into the arch.

## Working tints

| Slot | Working value |
|---|---|
| Door inscription fill | `#DDD0AE` |
| Judgement bay light | `#E7D1AD` |
| Brains bay light | `#9AA77A` |
| This morning bay light | `#E4D3A3` as light only, 6–10% soft-light |
| Judgement numeral | `#E7D1AD` (optional nag: `#D77545`) |
| Held lip | `#D77545` once per bay at most |
| Health trouble | `#8B3A2A` rotten, `#B85C38` unhappy |
| Health steady | `#E4D3A3` okay |
| Health good | `#9AA77A` happy and thriving |

Three colour steps on the wall; the word plus the loop carry all five shrine bands.
