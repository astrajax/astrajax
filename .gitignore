.DS_Store
.cursor/mcp.json
.env
.env.*
!.env.example
node_modules/
__pycache__/
*.pyc
.next/

# Hyperagent runtime noise (not source of truth)
hyperagent/logs/

# Personal/admin PDFs (do not commit)
IHT435_*.pdf
.vercel

# Visual-iteration scratch screenshots (kept on disk, out of git)
website/e2e/artifacts/
website/.screenshots/

# TypeScript incremental build cache
website/tsconfig.tsbuildinfo

# macOS/sync duplicate artefacts ("file 2.ts") — never source of truth
*[ ]2.*

# Image-generation intermediates (the shipped asset sits beside them)
website/public/**/*.prev.*
website/public/**/*.generated-raw.*

# fal / Milo previz outputs (keep folder, ignore media)
brand/.previz/*
!brand/.previz/.gitkeep
scripts/fal/_previz_env_bridge.py
