# Superseded Curator build packs (Cursor registry)

Historical build packs from when Curator was iterated in Cursor before Hyperagent
V5 became production.

**Do not use for new builds.** Live Curator spec:
`agents/hyperagent/clive/curator/build-pack-v5.md`

| File | Notes |
|---|---|
| `build-pack-v2.md` | V2 curator — proposal + Airtable create |
| `build-pack-v3.md` | V3 — daily briefings added |
| `build-pack-v4.md` | V4 — context hygiene auditor pivot |

Generator scripts `build_clive_curator_v2.py` … `v4.py` remain in `hyperagent/builds/` for the version chain consumed by `build_clive_curator_v5.py`.
